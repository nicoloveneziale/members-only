const router = require("express").Router();
const passport = require("passport");

// POST routes
router.post("/login");

router.post("register");

// GET routes
router.get("/");

router.get("/login");

router.get("register");
