<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create a Message</title>
    <link rel="stylesheet" href="/styles.css">
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@400;600;700&display=swap" rel="stylesheet">
</head>
<body>
    <div class="container">
        <div class="form-container create-message-form-container">
            <h1 class="form-title create-message-title">Create a Message</h1>
            <form action="/messages/create" method="POST" class="form create-message-form">
                <div class="form-group">
                    <label for="title" class="form-label">Title</label>
                    <input id="title" name="title" placeholder="Message Title" type="text" class="form-input" />
                </div>
                <div class="form-group">
                    <label for="text" class="form-label">Message Text</label>
                    <input id="text" name="text" placeholder="Maximum 400 characters" type="text" class="form-input" />
                </div>
                <% if (locals.errors) { %>
                    <ul class="form-errors">
                        <% errors.forEach(function(error) { %>
                            <li class="form-error-item"><%= error.msg %></li>
                        <% }); %>
                    </ul>
                <% } %>
                <button type="submit" class="form-button create-message-button">Post</button>
            </form>
        </div>
    </div>
</body>
</html>


